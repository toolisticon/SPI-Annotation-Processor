Skip to content
Search or jump to…
Pull requests
Issues
Marketplace
Explore
 
@tobiasstamann 
toolisticon
/
annotation-processor-toolkit
11
19
2
Code
Issues
4
Pull requests
11
Actions
Security
Insights
Settings
annotation-processor-toolkit/.github/workflows/jacoco.yml
@tobiasstamann
tobiasstamann Update jacoco.yml
Latest commit 9d1f5d9 39 minutes ago
 History
 1 contributor
41 lines (32 sloc)  869 Bytes
  
name: jacoco

on:
  push:
    branches:
      - '*'
      - '**/*'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v1

      - name: Set up JDK 8
        uses: actions/setup-java@v1
        with:
          java-version: 1.8

      - name: Cache .m2
        uses: actions/cache@v1
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven
            
      - name: Prepare mvnw
        run: chmod +x ./mvnw

      - name: Build
        run: ./mvnw verify jacoco:report-aggregate -B -q

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1.0.2
        with:
          token: ${{secrets.CODECOV_TOKEN}}
          file: target/site/jacoco-aggregate/jacoco.xml
Loading complete
